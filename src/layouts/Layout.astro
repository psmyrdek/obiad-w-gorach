---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Lunch menu z restauracji w okolicy" />
    <title>{title}</title>
  </head>
  <body class="bg-gray-50 min-h-screen">
    <header class="bg-white border-b border-gray-200">
      <div class="max-w-2xl mx-auto px-4 py-4">
        <h1 class="text-xl font-bold text-gray-900">Obiad w Górach 🏔️</h1>
        <p class="text-sm text-gray-500">Menu lunchowe na ten tydzień</p>
      </div>
    </header>
    <main class="max-w-2xl mx-auto px-4 py-6">
      <slot />
    </main>
  </body>
</html>
